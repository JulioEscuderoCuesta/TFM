# Storm Configuration

# Local directory configuration
storm.local.dir: "storm-local"

# UI configuration
ui.port: 8080

storm.zookeeper.servers:

storm.zookeeper.port: 2181

# Supervisor configuration
        supervisor.slots.ports:
        - 6700
        - 6701
        - 6702
        - 6703

# Nimbus configuration
nimbus.seeds: ["NimbusNode"]
